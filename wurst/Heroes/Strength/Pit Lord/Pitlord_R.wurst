package Pitlord_R

import DamageTarget

public function pitLordThorn(unit holder, unit attacker)
    let dam = 60. * holder.getAbilityLevel('A0RI')
    holder.damageTargetCode(dam, attacker, null, -1, damageType.PhysicalRanged)