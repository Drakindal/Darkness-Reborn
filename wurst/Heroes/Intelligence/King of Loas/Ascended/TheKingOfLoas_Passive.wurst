package TheKingOfLoas_Passive

import ClosureEvents
import MyAbilityIds

public function crownOfRezan()
    EventListener.add(EVENT_PLAYER_UNIT_DEATH) -> 
        let u = GetKillingUnit()
        if u.getAbilityLevel(KING_OF_LOAS_ASC_ABILITY_6) >= 1
            u.addInt(2)