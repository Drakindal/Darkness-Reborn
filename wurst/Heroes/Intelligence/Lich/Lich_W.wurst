package Lich_W

import ClosureEvents
import MyAbilityIds

public function frostArmor()
    EventListener.onTargetCast(LICH_ABILITY_2) (unit caster, unit target) ->
        let heal = (caster.getAbilityLevel(LICH_ABILITY_2) * 2.00) * caster.getInt(true)
        target.addHP(heal)