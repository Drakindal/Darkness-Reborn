package DarkRanger_R

import MyAbilityIds
import MyHeroAbilityDefinition
import MyUnitIds
import ClosureEvents

public function burstMana()
    EventListener.onCast(DARK_RANGER_ABILITY_4) (unit caster) ->
        let lvl = GetUnitAbilityLevel(caster, DARK_RANGER_ABILITY_4)
        let hp = caster.getHP()
        let calc = 1-(0.04*lvl)
        let newHp = calc*hp
        let newMp = (newHp * (lvl * 0.01))
        caster.setHP(newHp)
        caster.addMana(newMp)