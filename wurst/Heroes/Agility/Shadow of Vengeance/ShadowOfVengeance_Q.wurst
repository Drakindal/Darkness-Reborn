package ShadowOfVengeance_Q

import DummyCaster
import OrderIds
import MyAbilityIds
import DamageTarget
import ClosureEvents
import DamageCustomEvent
import MyBuffIds

public function furyPeak()
    EventListener.onCast(SHADOW_OF_VENGEANCE_ABILITY_1) (unit caster) ->
        if caster.hasAbility(SHADOW_OF_VENGEANCE_BUFF_5)
            caster.removeAbility(SHADOW_OF_VENGEANCE_BUFF_5)
    DamageCustomEvent.addListener(2) ->
        let src = DamageCustomEvent.getSource()
        if src.isIllusion()
            skip
        else
            if (DamageCustomEvent.getType() == ATTACK)
                let damtyp = DamageCustomEvent.getDamageType()
                if (damtyp == DAMAGE_TYPE_NORMAL or damtyp == DAMAGE_TYPE_MAGIC)
                    if src.hasAbility(SHADOW_OF_VENGEANCE_BUFF_1)
                        let targ = DamageCustomEvent.getTarget()
                        let missingHp = src.getMissingHP()
                        new DummyCaster()
                        ..owner(GetOwningPlayer(src))
                        ..origin(src.getPos())
                        ..castTarget(SHADOW_OF_VENGEANCE_ABILITY_1_DUMMY, 1, OrderIds.thunderbolt, targ)
                        src.damageTargetCode(missingHp, targ, null, -1, customDamageType.Magical)